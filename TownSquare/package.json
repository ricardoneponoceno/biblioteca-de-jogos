{
  "name": "townsquare",
  "version": "0.1.0",
  "private": true,
  "description": "Embeddable TownSquare slice: self-hostable realtime presence, walking, chat, and a default scene.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && find public -name '*.mjs' -exec node --check {} \\;",
    "smoke": "node scripts/smoke-test.js"
  },
  "dependencies": {
    "ws": "^8.18.3"
  },
  "engines": {
    "node": ">=18"
  }
}
