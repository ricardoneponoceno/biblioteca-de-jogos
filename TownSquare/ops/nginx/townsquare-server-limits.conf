# Include in the TownSquare server block. Empty keys make these /live-only.
limit_conn townsquare_live_connections 2;
limit_req zone=townsquare_live_handshakes burst=15 nodelay;
limit_conn_status 429;
limit_req_status 429;
