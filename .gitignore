# Dependências do Node.js
/backend/node_modules

# Ficheiro de variáveis de ambiente do Backend
/backend/.env
/backend/.env.local

# Arquivos de variáveis de ambiente dos proxies RAWG/HLTB
/api-rawg/.env
/api-hltb/.env

# Dump de dado real de produção (catálogo legado pro backfill do #15) — passado
# fora do git, nunca versionado
/backend/legado-jogos-generos.sql

# Ficheiros de log e sistema
npm-debug.log
.DS_Store

# Dados da Base de Dados
/postgres-data/

# Ficheiro de configuração do Frontend (NÃO VERSIONAR)
/frontend/config.js

# Notas e propostas pessoais de desenvolvimento (não faz parte do repo original)
/notas-marcelo/

# Contexto pessoal de agente de IA (não faz parte do repo original)
/AGENTS.md
/CLAUDE.md

# Ajustes pessoais de ambiente (porta local, etc.) — mesclado automaticamente pelo Docker Compose
/docker-compose.override.yml
